# First public package: TRACER 0.8.8

This file describes the supplied package. It does not assert that a GitHub Release has already been published.

| Item | Verified value |
| --- | --- |
| Release tag to create | `v0.8.8` |
| Release title | `TRACER 0.8.8 — Windows (64-bit)` |
| Channel | Beta; mark the GitHub release as a pre-release |
| Download asset | `TRACER_v0.8.8.zip` |
| ZIP size | 62,646,868 bytes (59.7 MiB) |
| Archive contents | One file: `TRACER_v0.8.8.exe` |
| Target | Windows x64; PE machine `0x8664` |
| SHA-256 | `d3b00e428400bef8c0b0514b713f0a12ff677a6c9b7a50f5172d1cb76913ad22` |

The ZIP integrity check passes. The executable was inspected but was not run or functionally tested on Windows.

## Release description

Download **TRACER_v0.8.8.zip** for **64-bit Windows (Intel or AMD)**.

1. Download the ZIP from the Assets list below.
2. Right-click it and choose **Extract All**.
3. Open the extracted folder and double-click **TRACER_v0.8.8.exe**.

This is a beta build. No Mac or Linux package is included in this release. The automatically generated Source code archives are not the TRACER application.

## Publication checklist

1. Create a draft pre-release tagged `v0.8.8`, targeting the reviewed commit on `main`.
2. Attach the original `TRACER_v0.8.8.zip` unchanged and copy the release description above into the release notes.
3. Confirm the asset size and SHA-256 match this table, and finish the appropriate Windows functional test.
4. Publish the release after its upload completes. Run **Publish release catalog** manually on `main` if needed.
5. Check the README and website both show the Windows 64-bit download and that its URL downloads the ZIP successfully.

GitHub release publication is distinct from merging the repository changes. The workflow intentionally lists only published assets.
